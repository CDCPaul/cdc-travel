export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="w-full min-h-screen bg-gray-50 m-0 p-0 flex items-center justify-center">
      {children}
    </div>
  );
} 